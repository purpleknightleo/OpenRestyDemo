This project is a demo for OpenResty

1. use require to import custom lib
2. redis 
3. mysql

curl 'localhost:8080/add?a=8&b=3'    
curl 'localhost:8080/cmd=get&key=a'   
curl 'localhost:8080/mysql'     
